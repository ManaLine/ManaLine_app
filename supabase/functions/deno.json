{
  "// note": "Edge Function workspace config. Supabase deploys each function with its own Deno runtime and does not read a test task, so this exists for LOCAL typechecking and unit tests only — `deno task check` and `deno task test` from supabase/functions/.",
  "tasks": {
    "check": "deno check _shared/*.ts */index.ts",
    "test": "deno test _shared/"
  },
  "fmt": {
    "lineWidth": 100,
    "semiColons": true,
    "singleQuote": false
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
